import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.reactcapacitor2026.app',
  appName: 'React Capacitor 2026',
  webDir: 'dist',
}

export default config
